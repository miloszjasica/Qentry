<<<<<<< HEAD
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:components="clr-namespace:Qentry.Views.Controls"
             xmlns:viewmodels="clr-namespace:Qentry.ViewModels"
             xmlns:views="clr-namespace:Qentry.Views"
             x:Class="Qentry.Views.MyEventsPage"
             BackgroundColor="{StaticResource BgColor}"
             x:Name="MyEventsPageInstance">

    <Grid RowDefinitions="*, Auto">
		<ScrollView>
=======
<ContentPage
    x:Class="Qentry.Views.MyEventsPage"
    x:Name="MyEventsPageInstance"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:components="clr-namespace:Qentry.Views.Controls"
    xmlns:viewmodels="clr-namespace:Qentry.ViewModels"
    xmlns:views="clr-namespace:Qentry.Views"
    xmlns:converters="clr-namespace:Qentry.Converters"
    BackgroundColor="{StaticResource BgColor}">

    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:InverseBoolConverter x:Key="InverseBoolConverter" />
            <converters:BoolToOpacityConverter x:Key="BoolToOpacityConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="*,Auto">

        <ScrollView>
>>>>>>> patrykjas
            <VerticalStackLayout>

                <!-- Kalendarz -->
                <Grid Margin="0,50,0,0">
                    <views:HorizontalCalendarView
                        x:Name="Calendar"
                        SelectedDay="{Binding SelectedDay, Mode=TwoWay}" />
                </Grid>

                <!-- Pole wyszukiwania -->
                <Border
                    Margin="30,20"
                    Stroke="{StaticResource Primary}"
                    StrokeThickness="1"
                    StrokeShape="RoundRectangle 28">

                    <Grid
                        HeightRequest="40"
                        Padding="20,0,0,0"
                        BackgroundColor="Transparent"
                        ColumnDefinitions="Auto,*,Auto">

                        <ImageButton
                            Source="filter.png"
                            WidthRequest="24"
                            HeightRequest="24"
                            BackgroundColor="Transparent"
                            Command="{Binding ToggleFilterPanelCommand}" />

                        <Entry
                            x:Name="SearchEntry"
                            Grid.Column="1"
                            Text="{Binding SearchQuery, Mode=TwoWay}"
                            Placeholder="Wyszukaj wydarzenie"
                            TextColor="Black" />

                        <ImageButton
                            Grid.Column="2"
                            Source="search.png"
                            WidthRequest="24"
                            HeightRequest="24"
                            BackgroundColor="Transparent"
                            Command="{Binding SearchCommand}" />
                    </Grid>
                </Border>
<<<<<<< HEAD

                <StackLayout IsVisible="{Binding IsFilterPanelVisible}"
                             Padding="20"
                             Spacing="10">

                    <Label Text="Filtruj według lokalizacji:"
                           FontAttributes="Bold"
                           TextColor="Black"/>
=======

                <!-- Panel filtrów -->
                <StackLayout
                    Padding="20"
                    Spacing="10"
                    IsVisible="{Binding IsFilterPanelVisible}">

                    <Label
                        Text="Filtruj według lokalizacji:"
                        FontAttributes="Bold"
                        TextColor="Black" />

                    <Entry
                        Text="{Binding LocationFilter, Mode=TwoWay}"
                        Placeholder="Wpisz lokalizację"
                        TextColor="Black" />

                    <Label
                        Text="Filtruj po dacie:"
                        FontAttributes="Bold"
                        TextColor="Black" />

                    <Entry
                        Text="{Binding ManualDateFilter, Mode=TwoWay}"
                        Placeholder="Wpisz datę (RRRR-MM-DD)"
                        Keyboard="Chat"
                        TextColor="Black" />
>>>>>>> patrykjas

                    <Button
                        Text="Resetuj filtry"
                        WidthRequest="150"
                        Margin="0,10,0,0"
                        BackgroundColor="{StaticResource Color2}"
                        TextColor="Black"
                        CornerRadius="10"
                        Command="{Binding ResetFiltersCommand}" />
                </StackLayout>

<<<<<<< HEAD
                <!-- Pole Eventow -->
                <Border Background="{StaticResource Primary}"
                        Stroke="{StaticResource Primary}"
                        StrokeThickness="2"
                        MinimumHeightRequest="{Binding ScreenHeight}"
                        StrokeShape="RoundRectangle 50, 50, 0, 0"
                        Margin="0, 0, 0, -1">

                    <Grid>
                        <VerticalStackLayout Margin="0, 20, 0, 0">

                            <!-- Pole przyciskow -->
                            <Border HeightRequest="60"
=======
                <!-- Pole wydarzeń -->
                <Border
                    Margin="0,0,0,-1"
                    Background="{StaticResource Primary}"
                    Stroke="{StaticResource Primary}"
                    StrokeThickness="2"
                    MinimumHeightRequest="{Binding ScreenHeight}"
                    StrokeShape="RoundRectangle 50,50,0,0">

                    <Grid>
                        <VerticalStackLayout Margin="0,20,0,0">

                            <!-- Przełącznik zakładek -->
                            <Border
                                HeightRequest="60"
                                Stroke="Transparent"
                                StrokeShape="RoundRectangle 50">

                                <Border
                                    WidthRequest="355"
                                    Padding="3"
                                    HorizontalOptions="Center"
                                    BackgroundColor="{StaticResource Quaternary}"
>>>>>>> patrykjas
                                    Stroke="Transparent"
                                    StrokeThickness="0"
                                    StrokeShape="RoundRectangle 50">

<<<<<<< HEAD
                                <Border BackgroundColor="{StaticResource Quaternary}"
                                        WidthRequest="355"
                                        StrokeShape="RoundRectangle 50"
                                        Stroke="Transparent"
                                        StrokeThickness="0"
                                        HorizontalOptions="Center"
                                        Padding="3">

                                    <Grid ColumnDefinitions="*,*">
                                        <Border BackgroundColor="{Binding IsCreatedSelected, Converter={StaticResource BoolToColorConverter}, ConverterParameter='Selected'}"
                                                StrokeShape="RoundRectangle 50"
                                                StrokeThickness="0"
                                                HorizontalOptions="Fill"
                                                VerticalOptions="Fill">
=======
                                    <Grid>

                                        <!-- Utworzone -->
                                        <Border
                                            BackgroundColor="{Binding IsCreatedSelected, Converter={StaticResource BoolToColorConverter}, ConverterParameter=Selected}"
                                            StrokeThickness="0"
                                            StrokeShape="RoundRectangle 50">

>>>>>>> patrykjas
                                            <Border.GestureRecognizers>
                                                <TapGestureRecognizer Command="{Binding ShowCreatedCommand}" />
                                            </Border.GestureRecognizers>

                                            <Label
                                                Text="Utworzone wydarzenia"
                                                FontAttributes="Bold"
                                                HorizontalOptions="Center"
                                                VerticalOptions="Center"
                                                TextColor="{Binding IsCreatedSelected, Converter={StaticResource BoolToTextColorConverter}}" />
                                        </Border>

                                    </Grid>
<<<<<<< HEAD

                                </Border>
                            </Border>
                            <CollectionView ItemsSource="{Binding Events}"
                                            IsVisible="{Binding IsCreatedSelected}"
                                            SelectionMode="Single"
                                            SelectionChanged="OnEventSelected"
                                            VerticalOptions="Start">
=======
                                </Border>
                            </Border>

                            <!-- Lista wydarzeń -->
                            <CollectionView
                                ItemsSource="{Binding Events}"
                                IsVisible="{Binding IsCreatedSelected}"
                                SelectionMode="Single"
                                SelectionChanged="OnEventSelected"
                                VerticalOptions="Start">
>>>>>>> patrykjas

                                <CollectionView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Padding="0,4"
                                              IsEnabled="{Binding IsExpired, Converter={StaticResource InverseBoolConverter}}"
                                              Opacity="{Binding IsExpired, Converter={StaticResource BoolToOpacityConverter}}">

                                            <Border
                                                Margin="12,6"
                                                Padding="14"
                                                Background="#4A4350"
                                                Stroke="#3A3341"
                                                StrokeThickness="1"
                                                StrokeShape="RoundRectangle 20"
                                                Shadow="10,10,15,#00000040">

                                                <Grid ColumnDefinitions="160,*,Auto">

                                                    <Border
                                                        Grid.Column="0"
                                                        WidthRequest="150"
                                                        HeightRequest="100"
                                                        BackgroundColor="#00000030"
                                                        StrokeThickness="0"
                                                        StrokeShape="RoundRectangle 16">

                                                        <Image
                                                            Source="{Binding Image}"
                                                            Aspect="AspectFill"
                                                            WidthRequest="150"
                                                            HeightRequest="100" />
                                                    </Border>

                                                    <VerticalStackLayout
                                                        Grid.Column="1"
                                                        Margin="18,0,0,0"
                                                        Spacing="4"
                                                        VerticalOptions="Center">

                                                        <Label
                                                            Text="{Binding Name}"
                                                            FontSize="18"
                                                            FontAttributes="Bold"
                                                            TextColor="White" />

                                                        <Label
                                                            Text="{Binding Location}"
                                                            FontSize="14"
                                                            TextColor="#CFCFCF" />

                                                        <Label
                                                            Text="{Binding StartDate, StringFormat='{0:dd.MM.yyyy}'}"
                                                            FontSize="14"
                                                            TextColor="#CFCFCF" />
                                                    </VerticalStackLayout>

                                                    <VerticalStackLayout
                                                        Grid.Column="2"
                                                        HorizontalOptions="End"
                                                        VerticalOptions="Center">

<<<<<<< HEAD
                                                        <Button Text="Usuń"
                                                                BackgroundColor="#7A3D87"
                                                                TextColor="White"
                                                                CornerRadius="12"
                                                                Padding="20,8"
                                                                HeightRequest="38"
                                                                WidthRequest="90"
                                                                FontSize="14"
                                                                Shadow="5,5,10,#00000040"
                                                                Command="{Binding Source={x:Reference MyEventsPageInstance}, Path=BindingContext.DeleteEventCommand}"
                                                                CommandParameter="{Binding Id_Event}"
                                                                IsVisible="{Binding Source={x:Reference Calendar}, Path=BindingContext.IsOrganizer}"/>
=======
                                                        <Button
                                                            Text="Usuń"
                                                            WidthRequest="90"
                                                            HeightRequest="38"
                                                            Padding="20,8"
                                                            FontSize="14"
                                                            CornerRadius="12"
                                                            BackgroundColor="#7A3D87"
                                                            TextColor="White"
                                                            Shadow="5,5,10,#00000040"
                                                            Command="{Binding Source={x:Reference MyEventsPageInstance}, Path=BindingContext.DeleteEventCommand}"
                                                            CommandParameter="{Binding Id_Event}"
                                                            IsVisible="{Binding Source={x:Reference Calendar}, Path=BindingContext.IsOrganizer}"
                                                            IsEnabled="{Binding IsExpired, Converter={StaticResource InverseBoolConverter}}" />
>>>>>>> patrykjas
                                                    </VerticalStackLayout>

                                                </Grid>
                                            </Border>
                                        </Grid>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>

                            <!-- Brak wydarzeń -->
                            <StackLayout
                                Margin="0,40"
                                HorizontalOptions="Center"
                                IsVisible="{Binding NoEventsVisible}">

<<<<<<< HEAD
                            <StackLayout IsVisible="{Binding IsArchivedSelected}" Margin="0,20,0,0">
=======
                                <Image
                                    Source="olowek.png"
                                    WidthRequest="450"
                                    HeightRequest="450"
                                    Opacity="0.6" />
>>>>>>> patrykjas

                                <Label
                                    Text="Nie masz żadnych wydarzeń."
                                    FontSize="18"
                                    TextColor="Black"
                                    HorizontalOptions="Center" />
                            </StackLayout>
<<<<<<< HEAD

                            <StackLayout IsVisible="{Binding NoEventsVisible}"
                                         HorizontalOptions="Center"
                                         Margin="0,40">

                                <Image Source="olowek.png"
                                       WidthRequest="450"
                                       HeightRequest="450"
                                       Opacity="0.6" />

                                <Label Text="Nie masz żadnych wydarzeń."
                                       FontSize="18"
                                       TextColor="Black"
                                       HorizontalOptions="Center"/>
                            </StackLayout>
                        </VerticalStackLayout>
                    </Grid>
                </Border>
                <Grid BackgroundColor="{StaticResource Primary}" HeightRequest="100"/>
=======

                        </VerticalStackLayout>
                    </Grid>
                </Border>

                <Grid
                    HeightRequest="100"
                    BackgroundColor="{StaticResource Primary}" />

>>>>>>> patrykjas
            </VerticalStackLayout>
        </ScrollView>

        <!-- Bottom navigation -->
        <components:CustomBottomNavBar VerticalOptions="End" />

    </Grid>
</ContentPage>
